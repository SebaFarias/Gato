class Nav{
    constructor(data){
        this.navElement = document.getElementById('navBar')
        this.data = data
        this.content = initialize(data)
    }
    initialize(data){
        let options = []
        data.options.map( option => {

        })
        return
    }
}